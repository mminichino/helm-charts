{{- if .Values.dns.domain }}
{{- if or (eq .Values.service.ingress.type "nginx") (eq .Values.service.ingress.type "haproxy") }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  {{- if eq .Values.service.ingress.type "haproxy" }}
  annotations:
    kubernetes.io/ingress.class: haproxy
    ingress.kubernetes.io/ssl-passthrough: "true"
  {{- else }}
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-passthrough: "true"
    nginx.ingress.kubernetes.io/backend-protocol: HTTPS
  {{- end }}
spec:
  rules:
    - host: "postgres.{{ .Values.dns.domain }}"
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: "postgres"
                port:
                  number: {{ .Values.service.clusterIP.port }}
{{- end }}
{{- end }}
