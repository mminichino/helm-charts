#
{{- if and .Values.tls.certificate .Values.tls.key }}
apiVersion: v1
kind: Secret
metadata:
  name: proxy-cert-secret
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  certificate: {{ .Values.tls.certificate }}
  key: {{ .Values.tls.key }}
  name: cHJveHk=
{{- end }}
